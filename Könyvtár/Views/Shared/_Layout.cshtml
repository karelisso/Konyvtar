<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>


    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/myscripts");
    @*@Scripts.Render("~/bundles/bootstrap")*@
    @Scripts.Render("~/bundles/knockout")

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href='http://fonts.googleapis.com/css?family=Oswald:400,300,700' rel='stylesheet' type='text/css'>
    <link href="https://fonts.googleapis.com/css2?family=Sriracha&display=swap" rel="stylesheet">

</head>
<body>

    <nav class="navbar navbar-expand-lg bg-dark navbar-dark text-white fixed-top">
        <div class="container-fluid">
            <button button class="navbar-toggler d-inline bg-warning rounded-pill me-2"  type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"
               <a href="#"><img src="~/szal/img/icon.png" alt="logo" width="60" ></a>
            </button>
            <ul class="navbar-nav navbar-collapse collapse" id="navbarSupportedContent">

                @if (Session["username"] == null)
                {
                    @*<li class="nav-item">
                @Html.ActionLink("Regisztráció", "RegisterUserPage", "Default", new { area = "" }, new { @class = "navbar-brand" })

            </li>*@
                    <li class="nav-item">
                        @Html.ActionLink("Bejelentkezés", "LogInPage", "Default", new { area = "" }, new { @class = "navbar-brand" })

                    </li>


                }
                else
                {

                    if (int.Parse(Session["level"].ToString()) > 1)
                    {
                        <li class="nav-item">
                            @Html.ActionLink("Regisztráció", "RegisterUserPage", "Default", new { area = "" }, new { @class = "navbar-brand" })

                        </li>
                    }
                    else
                    {

                    }



                    if (int.Parse(Session["level"].ToString()) >= 1)
                    {
                        @*@Html.ActionLink("Állomány növelés", "MetaPage", "Default", new { area = "" }, new { @class = "" })*@

                        <li class="nav-item dropdown">
                            <button class="navbar-brand dropdown-toggle btn text-white" style="font-family: 'Sriracha', cursive !important;" id="navalomany" data-bs-toggle="dropdown">
                                Állománykezelés
                            </button>
                            <div class="dropdown-menu" aria-labelledby="navalomany">
                                @Html.ActionLink("Állománynövelés", "CreateMetaPage", "Default", new { area = "" }, new { @class = "dropdown-item " })
                                @Html.ActionLink("Állománycsökkentés", "DeleteMetaPage", "Default", new { area = "" }, new { @class = "dropdown-item" })
                                @Html.ActionLink("Állománymódosítás", "ChangeMetaPage", "Default", new { area = "" }, new { @class = "dropdown-item" })

                            </div>
                        </li>

                        <li class="nav-item">
                            @Html.ActionLink("Könyvkeresés", "IndexPage", "Default", new { area = "" }, new { @class = "navbar-brand navbar-right" })
                        </li>

                        <li class="nav-item dropdown">
                            <button class="navbar-brand dropdown-toggle btn text-white" style="font-family: 'Sriracha', cursive !important;" id="navolvaso" data-bs-toggle="dropdown">
                                Kölcsönzések
                            </button>
                            <div class="dropdown-menu" aria-labelledby="navolvaso">
                                @Html.ActionLink("Tagfelvétel", "CreateReaderCardPage", "Default", new { area = "" }, new { @class = "dropdown-item" })
                                @Html.ActionLink("Taglista", "ManageReaderCardPage", "Default", new { area = "" }, new { @class = "dropdown-item" })
                                @Html.ActionLink("Saját profil", "ProfilePage", "Default", new { area = "" }, new { @class = "dropdown-item" })

                                @Html.ActionLink("Kölcsönzések kezelése", "RentReaderCardPage", "Default", new { area = "" }, new { @class = "dropdown-item" })
                            </div>
                        </li>


                        @*<li class="dropdown">
                    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
                        Dropdown Example
                        <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu">
                        <li><a href="#">HTML</a></li>
                        <li><a href="#">CSS</a></li>
                        <li><a href="#">JavaScript</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Állomány kezelése
                        </button>
                        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                            @Html.ActionLink("Állomány növekedés", "MetaPage", "Default", new { area = "" }, new { @class = "dropdown-item" })
                            @Html.ActionLink("Állomány törlése", "MetaPage", "Default", new { area = "" }, new { @class = "dropdown-item" })
                        </div>
                    </div>
                </li>*@
                    }
                    else
                    {
                        <li class="nav-item">
                            @Html.ActionLink("Könyvkeresés", "IndexPage", "Default", new { area = "" }, new { @class = "navbar-brand navbar-right" })
                        </li>

                        <li class="nav-item">
                            @Html.ActionLink("Saját profil", "ProfilePage", "Default", new { area = "" }, new { @class = "navbar-brand navbar-right" })

                        </li>
                    }


                    @*<li class="nav-item">
                        @Html.ActionLink("Súgó", "Helperpage", "Default", new { area = "" }, new { @class = "navbar-brand navbar-right" })

                    </li>*@

                    if (int.Parse(Session["level"].ToString()) >= 1)
                    {

                        <li class="nav-item">
                            @Html.ActionLink("Napló", "LogPage", "Default", new { area = "" }, new { @class = "navbar-brand" })

                        </li>

                    }

                    <li class="nav-item">
                        @Html.ActionLink("Kilépés", "startpage", "Default", new { area = "" }, new { @class = "navbar-brand" })
                    </li>



                }


            </ul>
        </div>

    </nav>


    <div class="container body-content">

        @if ( (Session["success"] != null || Session["error"] != null)  && (Session["success"].ToString().Length > 1 || Session["error"].ToString().Length > 1))
        {
            <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
                <div class="modal-dialog text-dark">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h4 class="modal-title" id="myModalLabel">Esemény</h4>
                            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                        </div>
                        <div class="modal-body text-dark">
                            @Session["error"]
                            <hr>
                            @Session["success"]
                        </div>
                    </div>
                </div>
            </div>
            <script>
                $(document).ready(function () {
                    $('#myModal').modal('show');
                    ResetSession("error");
                    ResetSession("success");
                });
            </script>
        }



        @RenderBody()
        <hr />
        <footer>
            <p class="text-white">&copy;2022- @DateTime.Now.Year </p>
        </footer>
    </div>

    <script>
        function toogelnav() {

            console.log("fgdsdf")
           // setInterval(2000);
          //  $("#navosbar").css("height", "33px");
        }

        function ResetSession(value) {
            $.ajax({
                url: "/Default/SetSession",
                type: "POST",
                data: { 'name': value, 'value': "" },
                success: function () {
                },
                error: function () { alert("error"); }
            });
        }

    </script>

    @RenderSection("scripts", required: false)
</body>
</html>