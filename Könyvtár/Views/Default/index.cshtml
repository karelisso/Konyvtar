using system.file;
@*@{
    Layout = null;
}*@
@{
    ActionResult kep;
    using (var voomy = new Könyvtár.App_Data.DefaultController())
    {
        kep = voomy.Load_Image();
    }
}
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    @if (Session["username"] == null)
    {
        <div>
            veeemo <br />
            @Html.ActionLink("Bejelentkezés", "Secnd");
        </div>
    }
    else
    {
        <div>
            username: <br />
            @(Session["username"]) <br>
            userid: <br />
            @(Session["userid"]) <br />
            usermail: <br />
            @(Session["usermail"]) <br />
            @Html.ActionLink("Bejelentkezés", "Secnd");
        </div>
    }
    @RenderPage("book_add.cshtml")
    @RenderPage("book_render.cshtml")
</body>
</html>
